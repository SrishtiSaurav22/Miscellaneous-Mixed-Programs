#include<bits/stdc++.h>

using namespace std;

void print_subsets(int index, string current_subset, string original_string)
{
    if(index=original_string.length()-1)
        cout<<(current_subet + original_string[index])
}

int main()
{
    int num=0;
    bool val=true;

    cout<<(num+val)<<endl;

    num=num+int(val);

    cout<<(num+val);

    return 0;
}